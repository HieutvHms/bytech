import 'package:flutter/material.dart';

class ScreneScreen extends StatelessWidget {
  const ScreneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Screne")),
    );
  }
}
